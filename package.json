{
  "name": "carbonmote_721",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ carbonmote_721",
    "create-local": "graph create --node http://localhost:8020/ carbonmote_721",
    "remove-local": "graph remove --node http://localhost:8020/ carbonmote_721",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 carbonmote_721",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.55.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
